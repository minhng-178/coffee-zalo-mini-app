{
  "name": "hdui-coffee",
  "private": true,
  "version": "1.1.0",
  "description": "HDUI Coffee",
  "repository": "",
  "license": "UNLICENSED",
  "scripts": {
    "client": "npm start --prefix client",
    "server": "npm run dev --prefix server",
    "weixin": "npm run dev:h5 --prefix weixin",
    "dev": "concurrently -n CLIENT,SERVER,WEIXIN -c blue,green,red \"npm:client\" \"npm:server\" \"npm:weixin\"",
    "deploy": "npm run deploy --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
