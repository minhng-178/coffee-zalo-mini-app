{
  "name": "hdui-coffee",
  "private": true,
  "version": "1.1.0",
  "description": "HDUI Coffee",
  "repository": "",
  "license": "UNLICENSED",
  "scripts": {
    "client": "npm start --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm:client\" \"npm:server\"",
    "deploy": "npm run deploy --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
